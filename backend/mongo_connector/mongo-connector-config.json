{
    "__comment": "so far there is no config, but this is where you can e.g. blacklist/whitelist",
    "verbosity": 2,
    "logging": {
    	"type": "stream"
    },
    "__namespaces": {
      "knexDB.user": true
    }



}