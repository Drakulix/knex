version: '3'

services:
    mongodb:
        image: mongo

    elasticsearch:
        image: elasticsearch
        ports:
            - "9200:9200"
    flask:
        build: ./backend
        links:
            - mongodb
            - elasticsearch
        ports:
            - "5000:5000"
        volumes:
            - ./backend:/backend
